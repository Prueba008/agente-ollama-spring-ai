meta {
  name: Rechazar pregunta vacía
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/agent/chat
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "conversationId": "bruno-validation-001",
    "question": ""
  }
}

tests {
  test("Bean Validation rechaza la pregunta", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
